/**
 * models the behaviour of the grid.
 * @internal
 */
import { GridOptions } from "./definitions";
import { Grid } from "@jsplumb/util";
export declare class GridProfile {
    grid: Grid;
    snap: boolean;
    fitGroupsToGrid: boolean;
    constructor(opts: GridOptions);
}
//# sourceMappingURL=grid-profile.d.ts.map